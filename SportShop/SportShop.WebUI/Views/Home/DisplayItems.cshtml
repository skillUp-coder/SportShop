@using SportShop.WebUI.ViewModel
@using SportShop.WebUI.Helpers
@using SportShop.WebUI.Models
@model SportShop.WebUI.ViewModel.SortCategoryViewModel

<link href="~/Content/css/neon-btn.css" rel="stylesheet" />

@section CartSummary
{
    @Html.Action("CartSummary", "ShoppingCart")
}

@Html.Action("Menu", "Home")
<div style="height:100px;"></div>
<div class="row">
    
    <div class="col-lg-12">
        <div class="features-icons-item mx-auto mb-5 mb-lg-0 mb-lg-3">
            @*@foreach (var item in Model.Items)
            {
                <h3>@Html.DisplayFor(model => item.ItemName)</h3>
                <p class="lead mb-0">@Html.DisplayFor(model => item.ItemCategory)</p>
                <h4>@item.Price.ToString("# $")</h4>
                <a class="neon-btn" href="~/ShoppingCart/AddToCart/@item.ItemId">
                    <span></span>
                    <span></span>
                    <span></span>
                    <span></span>
                    Add to Cart
                </a>
            }
        </div>
        <div style="top:200px;" class="btn-group">
            @Html.PageLinks(Model.Info, x => Url.Action("DisplayItems", new { page = x }))
        </div>*@

    </div>

</div>
@section typing_footer
{
    <section class="call-to-action text-white text-center">
        <div class="overlay"></div>
        <div class="container">
            <div class="row">
                <div class="col-xl-9 mx-auto">

                    <div class="intro">
                        <h1 class="mb-5">Here you can</h1>
                        <h1 class="typing mb-5">hi</h1>
                    </div>
                </div>
            </div>
        </div>
    </section>
}







